* swap two numbers with just `^`
* Find the unique number with all other dups, with just `^`
* implement pow with bit ops and `*`
* find the lowerest 2^n LT current number ```n | = n >> 1; n |= n >> 2; n |= n >> 4;....; n |= n >> 32```
