FF-A: DZY loves sequences
-------
Transform this max problem into a search problem. Look at the condition, what can you infer from the condition that can help break down the problem, aside from head-on dp?

283-C: removing columns
-------
You need to scan through columns, how do you keep track of partial same results,i.e., the current prefixes are same?

276-A: Bits
------
One option is to iterate on # of bit positons, starting from the leftmost, and keep track of prefix
Another one is the recursively define f(L, R)
