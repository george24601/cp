---
11926
sort the list by start, then scan it, conflict iff last end > first start
related: what if size = 1 B? => standard divide and conquer => each one no conflict, last end <= first start


----
11933
solve it differently:
1. convert to string, and scan through string, update end string as we go 
2. read bit 1 by 1, based on index, add the end number to each target


----
11988

Similar to the matching bracekts problem
we just need to insert to head/tail => so a linked list is enough
